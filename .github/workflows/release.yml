name: release

on:
  push:


jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: 'jruby-9.1.17.0'
      - name: Release war
        run: |
          bash build.sh
